[project]
name = "chatbot"
version = "0.1.0"
description = "Ein Chatbot ohne Sinn"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "openai>=2.41.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
